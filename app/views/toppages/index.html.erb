<% if logged_in? %>
  <div class="col-xs-6">
    <ul class="nav nav-tabs nav-justified">
      <li><a href="#">参加プロジェクト</a></li>
    </ul>
      <%= render 'projects/projects', projects: @as_projects %>
  </div>
  <div class="col-xs-6">
    <ul class="nav nav-tabs nav-justified">
      <li><a href="#">作成プロジェクト</a></li>
    </ul>
      <%= render 'projects/ownprojects', projects: @projects %>
    </div>
  </div>
<% else %>
　<div class="center jumbotron">
  　<div class="text-center">
    　<h1>タスクボード</h1>
    　<div class="row">
  　　　　<div class="col-md-6 col-md-offset-3">
   　      <%= form_for(:session, url: login_path) do |f| %>
            <div class="form-group">
     　    　   <%= f.label :email, 'Email' %>
     　    　   <%= f.email_field :email, class: 'form-control' %>
     　    　</div>
            <div class="form-group">
              <%= f.label :password, 'Password' %>
              <%= f.password_field :password, class: 'form-control' %>
            </div>
            <%= f.submit 'ログイン', class: 'btn btn-primary btn-block' %>
          <% end %>
        </div>
      </div>
      <br>
      <br>
      <br>
      <div class="row">
      　<div class="col-md-6 col-md-offset-3">
        　<%= link_to '新規登録', signup_path, class: 'btn btn-primary btn-block' %>
        </div>
      </div>
  　</div>
　</div>
<% end %>